export interface Work {
  id: string;
  slug: string;
  title: string;
  description: string;
  image: string;
}

export interface Character {
  id: string;
  name: string;
  group: string;
  description: string;
  images: string;
  skills: string;
}
